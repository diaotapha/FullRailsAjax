$("#produit_num_<%= @produit.id %>").replaceWith(" <%= raw escape_javascript render('produits/add_new', produit: @produit )%> ");

$('body').removeClass("modal-open");
$(".modal-backdrop").remove();
$(".backdrop_spinner").remove();
$(".loader_taph").remove();

swal({
toast: true,
position: 'center',
background: '#28a745',
showConfirmButton: false,
timer: 3000,
type: 'success',
title: "Produit mis à jour."
});